%div.row.well
  %div.col-md-2= link_to(@data_result.concert_date, "/concerts/#{@data_result.concert_id}")
  %div.col-md-2= @data_result.venue_name
  %div.col-md-3= @data_result.band_details
  %div.col-md-5
    = get_image_location(@data_result.concert_date, @data_result.bootleg_id, @data_result.bootleg_name, "normal")
    %ul
      %li= @data_result.band_name.gsub(/\\'/, '\'')
      %li= @data_result.bootleg_name.gsub(/\\'/, '\'')
      %li= turn_roio_type_into_icon(@data_result.roio_type).html_safe
      %li= @data_result.roio_format
      %li= @data_result.release_label.gsub(/\\'/, '\'')
      %li= @data_result.recorder.gsub(/\\'/, '\'')
      %li= @data_result.length
      %li= @data_result.cd
      %li= @data_result.taper.gsub(/\\'/, '\'')
      %li= @data_result.lineage.gsub(/\\'/, '\'')
      %li= @data_result.equipment.gsub(/\\'/, '\'')
      %li= @data_result.generation.gsub(/\\'/, '\'')
      %li= @data_result.taper_location.gsub(/\\'/, '\'')
      %li= @data_result.source_warning.gsub(/\\'/, '\'')
      %li= @data_result.user_name
      %li= turn_to_ratings_stars(@data_result.roio_avg_rating.to_s).html_safe
      %li= @data_result.md5